name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          echo "Installing dependencies..."
          sudo apt update
          sudo apt install -y build-essential ccache git zip unzip openjdk-11-jdk python3-pip python3-setuptools libncurses5 libstdc++6
          sudo apt install -y android-sdk android-ndk
          pip install --upgrade pip
          pip install buildozer
          buildozer --version  # للتحقق من النسخة المثبتة

      - name: Check Java version
        run: java -version  # للتأكد من أن Java مثبتة

      - name: Check Android SDK installation
        run: sdkmanager --list  # للتحقق من أن Android SDK تم تثبيته بشكل صحيح

      - name: Create buildozer.spec if not exists
        run: |
          if [ ! -f buildozer.spec ]; then buildozer init; fi

      - name: Build APK (without debug)
        run: |
          buildozer android release  # حاول البناء بشكل release بدلاً من debug

      - name: Check APK file existence
        run: |
          echo "Checking for APK files in bin/"
          ls -lh bin/  # تحقق من وجود ملفات الـ APK في مجلد bin/

      - name: Prepare APK for upload
        run: |
          mkdir -p artifact
          cp bin/*.apk artifact/
          ls -lh artifact/

      - name: Create archive for APK files
        run: |
          echo "Creating APK archive"
          tar -czf artifact.tar.gz -C artifact . 
          ls -lh artifact.tar.gz

      - name: Upload APK
        uses: actions/upload-artifact@v2
        with:
          name: guess-the-flag-apk
          path: artifact.tar.gz
